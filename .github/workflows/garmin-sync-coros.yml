name: garmin-coros-sync

on:
  workflow_dispatch:
  schedule:
    - cron: '0 4 * * *'
    - cron: '0 15 * * *'
env:
  GITHUB_NAME: njzyshare
  GITHUB_EMAIL: feimummy@126.com
  ## 佳明配置
  GARMIN_AUTH_DOMAIN: ${{ secrets.GARMIN_AUTH_DOMAIN }}
  GARMIN_EMAIL: ${{ secrets.GARMIN_EMAIL }}
  GARMIN_PASSWORD: ${{ secrets.GARMIN_PASSWORD }}
  GARMIN_NEWEST_NUM: ${{ secrets.GARMIN_NEWEST_NUM }}

  ## Coros配置
  COROS_EMAIL: ${{ secrets.COROS_EMAIL }}
  COROS_PASSWORD: ${{ secrets.COROS_PASSWORD }}

  # 启用 Node.js 24 运行 Actions
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Set Variables
        id: set_variables
        run: |
          echo "PY=$(python -c 'import hashlib, sys;print(hashlib.sha256(sys.version.encode()+sys.executable.encode()).hexdigest())')" >> $GITHUB_OUTPUT
          echo "PIP_CACHE=$(pip cache dir)" >> $GITHUB_OUTPUT
          echo "REQ_HASH=$(md5sum requirements.txt | cut -d' ' -f1)" >> $GITHUB_OUTPUT
      - name: Cache PIP
        id: cache-pip
        uses: actions/cache@v4
        with:
          path: ${{ steps.set_variables.outputs.PIP_CACHE }}
          key: Ubuntu-pip-${{ steps.set_variables.outputs.PY }}-${{ steps.set_variables.outputs.REQ_HASH }}
          restore-keys: |
            Ubuntu-pip-${{ steps.set_variables.outputs.PY }}-
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Download sync log
        uses: dawidd6/action-download-artifact@v6
        with:
          name: sync-log
          path: db/
          if_no_artifact_found: warn
      - name: Run Garmin Sync
        run: |
          python scripts/garmin/garmin_sync_coros.py
      - name: Upload sync log
        uses: actions/upload-artifact@v4
        with:
          name: sync-log
          path: db/sync_log.db
          overwrite: true
          retention-days: 1
      - name: Push new cookie
        run: |
          git config --local user.email "${{ env.GITHUB_EMAIL }}"
          git config --local user.name "${{ env.GITHUB_NAME }}"
          git add db/garmin_coros.db
          git commit -m 'update garmin coros sync' || echo "nothing to commit"
          git push || echo "nothing to push"
